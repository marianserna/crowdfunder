<%= form_for [project, comment] do |f| %>
<% comment.errors.full_messages.each do |m|%>
<p>
  <%= m %>
</p>
<% end %>
  <div class="field">
    <p><%= f.label :text %></p>
    <p><%= f.text_field :text  %></p>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
